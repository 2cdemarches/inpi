{
  "name": "inpi-token-bot",
  "version": "1.0.0",
  "description": "Bot qui renouvelle le token INPI toutes les 2h",
  "main": "refresh-inpi-token.js",
  "scripts": {
    "start": "node refresh-inpi-token.js",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.44.0",
    "dotenv": "^16.0.0"
  }
}
